import React from 'react';
import { Text, View, TouchableOpacity, Image, StatusBar, TextInput, ScrollView } from 'react-native';


const RiderHome = ({ navigation }) => {

    return (
        <View>
            <Text>This is rider portal</Text>
        </View>
    );
}

export default RiderHome;